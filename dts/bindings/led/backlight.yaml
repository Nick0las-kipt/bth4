title: LED Backlight

description: LED Backlight for LCD

compatible: "led-lcd-bl"

properties:
        pwms:
          type: phandle-array
          required: false
        gpios:
          type: phandle-array
          required: false
        label:
          type: string
          required: false
          description: Human readable string describing the device (used by Zephyr for API name)
